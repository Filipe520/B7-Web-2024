// Define a constante PI com o valor de Math.PI (valor de π)
let PI = Math.PI;

// Arredonda automaticamente o número 3.72 para o inteiro mais próximo
let arredondaAltomatico = Math.round(3.72);

// Arredonda o número 3.72 para cima, para o próximo inteiro
let arredondaPraCima = Math.ceil(3.72);

// Arredonda o número 3.72 para baixo, para o inteiro anterior
let arredondaPraBaiixo = Math.floor(3.72);

// Obtém o valor absoluto de -3.72 (remove o sinal negativo)
let numeroAbsoluto = Math.abs(-3.72);

// Retorna o menor número da lista fornecida
let qualMENORNumero = Math.min(2, 4, 5, 6, 1, 7, 8, 10);

// Retorna o maior número da lista fornecida
let qualMAIORNumero = Math.max(2, 4, 5, 6, 1, 7, 8, 600);

// Gera um número aleatório entre 1 e 10 (Math.random() gera entre 0 e 1)
let numeroAleatorio = Math.ceil(Math.random() * 10);

// Exibe o número aleatório gerado no console
console.log(numeroAleatorio);